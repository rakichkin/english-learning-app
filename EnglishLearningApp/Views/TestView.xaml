<UserControl x:Class="EnglishLearningApp.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EnglishLearningApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Word}" FontSize="30"/>
            <TextBox Text="{Binding Translation}" Height="100"/>
            <Button Content="Pass" Command="{Binding CheckAnswerCommand}" Height="100"/>
            <TextBlock Text="{Binding Result}" FontSize="30" />
            <Button Content="Next word" Command="{Binding NextWordCommand}" Height="100"/>
        </StackPanel>
    </Grid>
</UserControl>
